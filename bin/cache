#!/usr/bin/env bash
docker exec -u www-data -w /var/www/html $(docker-compose ps -q php) bin/console sw:cache:clear
docker exec -u www-data -w /var/www/html $(docker-compose ps -q php) bash -c 'var/cache/clear_cache.sh'
